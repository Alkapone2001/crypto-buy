{
  "name": "crypto-buy",
  "version": "1.0.0",
  "description": "Crypto-buy full-stack application",
  "private": true,
  "workspaces": [
    "front",
    "back"
  ],
  "scripts": {
    "install-all": "npm install && npm install --workspace=front && npm install --workspace=back",
    "build": "npm run build --workspace=front",
    "dev": "npm run dev --workspace=front & npm run dev --workspace=back"
  }
}
